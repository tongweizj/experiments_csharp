<Window x:Class="ManageOradersSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManageOradersSystem"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">


    <Grid Margin="16">

        <Grid.RowDefinitions>
            <RowDefinition Height = "80" />
            <RowDefinition Height = "80" />
            <RowDefinition Height = "50" />
            <RowDefinition Height = "*" />
           
            <RowDefinition Height = "50" />

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "*" />
            <ColumnDefinition Width = "*" />
            <ColumnDefinition Width = "*" />
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>


        <Label Content = "Orader Manager System" FontSize = "24" FontWeight = "Bold" Grid.ColumnSpan= "4" Grid.Row = "0"/>

        <StackPanel  Grid.Row = "1" Grid.Column="0"  Grid.ColumnSpan="3"  HorizontalAlignment="left" VerticalAlignment="Center">
           
            <TabControl>
                <TabItem Header="List Order Details">
                    
                    <TextBlock Text="List Order Details" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </TabItem>
                <TabItem Header="Add Item to Order">
                    <TextBlock Text="Add Item to Order" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </TabItem>
               
            </TabControl>
        </StackPanel>
        <GridSplitter Grid.Row="1"   Grid.ColumnSpan= "4" Height="2" HorizontalAlignment="Stretch" />
        <StackPanel  Grid.Row = "2" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="left">
            <TextBlock Margin="0" Text="Basket: "  FontSize = "16" Height="30"/>
            <ComboBox Name="BasketComboBox" Width="100" Height="30" Margin="0" SelectionChanged="BasketComboBox_SelectionChanged"/>
        </StackPanel>
        <StackPanel  Grid.Row = "2" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="right">
            <Button Content="Add Item to Order" Background="black" Foreground="White" FontSize="16" Margin="5"  Width="150" Height="30"
         Command="{Binding ClearBillCommand}" Click="Button_Click_1"/>
        </StackPanel>
        <StackPanel Grid.Row = "3" Grid.ColumnSpan="4">

            <DataGrid  x:Name="basketItemsDataGrid"  
                       ItemsSource="{Binding filterBasketItems}" 
                       AutoGenerateColumns="False"  
                       CanUserAddRows="False" 
                       BorderBrush="LightGray"          
                       BorderThickness="0.5"
                       GridLinesVisibility="All"
          HorizontalGridLinesBrush="LightGray"
          VerticalGridLinesBrush="LightGray"
                       >
                <DataGrid.Columns>
                    <DataGridTextColumn Width="*" Header="IdProduct" Binding="{Binding IdBasketItem}" IsReadOnly="True"/>
                    <DataGridTextColumn Width="*" Header="IdProduct" Binding="{Binding IdProduct}" IsReadOnly="True"/>
                    <DataGridTextColumn Width="150" Header="Quantity" Binding="{Binding Quantity}" IsReadOnly="True" />
                    <DataGridTextColumn Width="100" Header="IdBasket" Binding="{Binding IdBasket }"  IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row = "4" Grid.Column="2" Grid.ColumnSpan="2">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Delete Item" Background="Red" Foreground="White" FontSize="18" Margin="5"  Width="120" Height="40"
                        Command="{Binding RemoveSelectedItemsCommand}" Click="Button_Click"/>
               
            </StackPanel>

        </StackPanel>


    </Grid>
</Window>
